function showHidden ( password ) {
    if ( password == 'eugenel9929' ) {
        let navElement = document.querySelector('header nav ul');
        navElement.innerHTML += '<li> <a href="index_.html"> Private Space </a> </li>'
    }
    print('failed!');
}